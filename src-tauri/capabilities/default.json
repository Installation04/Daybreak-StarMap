{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The map window: core window permissions, plus opening the page's outbound links (wiki, papers, GitHub) in the default browser.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "opener:allow-open-url",
      "allow": [
        { "url": "https://**" },
        { "url": "http://**" }
      ]
    }
  ]
}
